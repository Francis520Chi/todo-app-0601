# 手動測試清單

這份文件用來記錄每次修改待辦清單 App 後，建議手動檢查的項目。

目標是讓你不用記住所有細節，只要照著清單一步一步確認即可。

## 測試前準備

- 用瀏覽器打開本機的 `index.html`，或打開 GitHub Pages 網址。
- 可先執行 `node tests/todo-logic.test.js`，確認資料邏輯自動化測試通過。
- 建議先準備 3 個測試任務，例如：
  - 工作：回覆 Email
  - 生活：買牛奶
  - 學習：練習 JavaScript
- 如果頁面上已經有舊任務，可以先按「清除全部」讓測試環境比較乾淨。

## 基本功能測試

- 網頁可以正常打開，沒有明顯排版錯誤。
- 可以輸入任務文字並新增任務。
- 空白任務不會被新增。
- 可以勾選任務為完成。
- 可以取消勾選，讓任務回到未完成。
- 可以編輯任務文字。
- 編輯時如果按取消，原本任務不會改變。
- 可以刪除單一任務。
- 可以清除全部任務。
- 清除全部前會出現確認訊息。

## 任務分類測試

- 新增任務時可以選擇分類。
- 任務列表會顯示分類。
- 可以新增「工作」分類任務。
- 可以新增「生活」分類任務。
- 可以新增「學習」分類任務。
- 可以新增「其他」分類任務。
- 編輯任務文字時，不會改變任務分類。
- 重新整理頁面後，任務分類仍會保留。

## 統計功能測試

- 新增任務後，「全部」數量會增加。
- 勾選任務後，「已完成」數量會增加。
- 勾選任務後，「未完成」數量會減少。
- 取消勾選後，統計數字會正確更新。
- 刪除任務後，統計數字會正確更新。
- 清除全部後，全部、已完成、未完成數量都會回到 0。

## 狀態篩選測試

- 點「全部」會顯示所有任務。
- 點「未完成」只會顯示未完成任務。
- 點「已完成」只會顯示已完成任務。
- 在篩選狀態下，勾選、編輯、刪除任務仍可以正常運作。

## 分類篩選測試

- 選「全部分類」會顯示所有分類的任務。
- 選「工作」時，只會顯示工作任務。
- 選「生活」時，只會顯示生活任務。
- 選「學習」時，只會顯示學習任務。
- 選「其他」時，只會顯示其他任務。
- 分類篩選可以和「全部 / 未完成 / 已完成」一起使用。

## 搜尋功能測試

- 輸入關鍵字後，可以找到符合文字的任務。
- 搜尋英文大小寫不同時，仍可以正常搜尋。
- 清空搜尋框後，任務列表會恢復。
- 搜尋和狀態篩選可以一起運作。
- 搜尋和分類篩選可以一起運作。
- 搜尋不到結果時，畫面不會壞掉。

## 深色模式測試

- 可以切換到深色模式。
- 可以切回淺色模式。
- 深色模式下文字仍清楚可讀。
- 重新整理頁面後，主題設定仍會保留。

## 建立日期測試

- 新增任務後，任務會顯示建立日期。
- 重新整理頁面後，建立日期仍會保留。
- 編輯任務文字時，不會改變建立日期。

## 匯出 / 匯入測試

- 可以將目前任務匯出成 JSON 檔案。
- 匯出的 JSON 檔案包含任務文字、分類、完成狀態和建立日期。
- 可以選擇 JSON 備份檔匯入任務。
- 匯入前會提醒目前任務將被取代。
- 匯入後，任務列表會更新成匯入的資料。
- 匯入後，統計、搜尋、狀態篩選和分類篩選仍可以正常運作。
- 匯入格式錯誤的檔案時，畫面不會壞掉，並會看到錯誤提示。

## 任務排序測試

- 完整列表狀態下會顯示上移和下移按鈕。
- 第二筆任務可以上移。
- 第二筆任務可以下移。
- 第一筆任務不能再上移。
- 最後一筆任務不能再下移。
- 重新整理後，任務順序仍會保留。
- 搜尋狀態下不顯示排序按鈕。
- 未完成或已完成篩選狀態下不顯示排序按鈕。
- 分類篩選不是「全部分類」時，不顯示排序按鈕。
- 匯出和匯入後，任務順序仍會保留。

## localStorage 測試

- 新增任務後重新整理頁面，任務仍會保留。
- 勾選完成後重新整理頁面，完成狀態仍會保留。
- 編輯任務後重新整理頁面，新的任務文字仍會保留。
- 刪除任務後重新整理頁面，被刪除的任務不會回來。
- 清除全部後重新整理頁面，任務仍是空的。
- 切換深色模式後重新整理頁面，主題設定仍會保留。

## 自動化測試

- 可以執行 `node tests/todo-logic.test.js`。
- 測試會檢查分類正規化。
- 測試會檢查匯入資料標準化。
- 測試會檢查錯誤日期處理。
- 測試會檢查備份檔名日期格式。

## 建議完整測試流程

1. 打開 `index.html`。
2. 清除全部任務。
3. 新增 3 個不同分類的任務。
4. 勾選其中 1 個任務為完成。
5. 編輯其中 1 個任務文字。
6. 刪除其中 1 個任務。
7. 確認統計數字正確。
8. 測試「全部 / 未完成 / 已完成」篩選。
9. 測試分類篩選。
10. 測試搜尋任務。
11. 回到完整列表狀態，測試任務上移和下移。
12. 切換深色模式。
13. 重新整理頁面，確認任務順序和深色模式仍保留。
14. 匯出目前任務資料。
15. 清除全部任務。
16. 匯入剛才匯出的 JSON 檔案。
17. 確認任務資料和排序成功回復。

## 測試完成後

- 如果所有功能都正常，可以進行 commit。
- 如果有功能壞掉，先記錄是哪一個步驟失敗，再回頭檢查相關程式。
