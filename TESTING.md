# 手動測試清單

這份文件用來記錄每次修改或上傳前，應該手動檢查的項目。

## 測試前準備
- 打開本機 `index.html`，或打開 GitHub Pages 網址。
- 建議使用一組新的測試任務。
- 測試前可以先清除全部任務，避免舊資料影響判斷。

## 基本功能測試
- 可以新增任務。
- 可以勾選任務為完成。
- 可以取消勾選，讓任務回到未完成。
- 可以編輯任務文字。
- 可以刪除單一任務。
- 可以清除全部任務。

## 統計功能測試
- 全部任務數量正確。
- 已完成任務數量正確。
- 未完成任務數量正確。
- 新增任務後，統計會更新。
- 勾選或取消完成後，統計會更新。
- 刪除任務後，統計會更新。
- 清除全部後，統計會回到 0。

## 篩選功能測試
- 「全部」會顯示所有任務。
- 「未完成」只會顯示未完成任務。
- 「已完成」只會顯示完成任務。
- 在篩選狀態下新增任務，畫面行為合理。
- 在篩選狀態下勾選、編輯、刪除任務，功能仍正常。

## 搜尋功能測試
- 輸入關鍵字後，可以找到符合文字的任務。
- 清空搜尋框後，任務列表會恢復。
- 搜尋和篩選可以一起運作。
- 搜尋不到結果時，畫面不會壞掉。

## 深色模式測試
- 可以切換到深色模式。
- 可以切回淺色模式。
- 重新整理後，主題設定仍保留。
- 深色模式下文字仍清楚可讀。

## 建立日期測試
- 新任務會顯示建立日期。
- 重新整理後，建立日期仍保留。
- 編輯任務文字時，不會改變建立日期。
- 舊任務沒有建立日期時，不會造成錯誤。

## 任務分類測試
- 新增任務時可以選擇分類。
- 任務列表會顯示分類。
- 編輯任務文字時，不會改變分類。
- 重新整理後，分類仍保留。
- 匯出的 JSON 檔案包含分類。
- 匯入 JSON 後，分類仍保留。
- 舊任務沒有分類時，會顯示為「其他」。

## 分類篩選測試
- 「全部分類」會顯示所有分類的任務。
- 選「工作」時，只會顯示工作任務。
- 選「生活」時，只會顯示生活任務。
- 選「學習」時，只會顯示學習任務。
- 選「其他」時，只會顯示其他任務。
- 分類篩選可以和全部、未完成、已完成篩選一起運作。
- 分類篩選可以和搜尋功能一起運作。
- 匯入 JSON 後，分類篩選仍正常。

## 任務排序測試
- 完整列表狀態下會顯示上移和下移按鈕。
- 第二筆任務可以上移。
- 第二筆任務可以下移。
- 第一筆任務不能再上移。
- 最後一筆任務不能再下移。
- 重新整理後，任務順序仍保留。
- 搜尋狀態下不顯示排序按鈕。
- 未完成或已完成篩選狀態下不顯示排序按鈕。
- 分類篩選不是「全部分類」時，不顯示排序按鈕。
- 匯出和匯入後，任務順序仍保留。

## 匯出 / 匯入測試
- 可以將目前任務匯出成 JSON 檔案。
- 匯出的 JSON 檔案包含任務文字、分類、完成狀態和建立日期。
- 可以選擇 JSON 備份檔匯入任務。
- 匯入前會提醒目前任務將被取代。
- 匯入後，任務列表、統計、篩選和搜尋仍正常。
- 匯入格式錯誤的檔案時，畫面不會壞掉。

## localStorage 測試
- 重新整理後，任務仍保留。
- 重新整理後，完成狀態仍保留。
- 重新整理後，深色模式仍保留。
- 不同瀏覽器資料不一致是正常行為。

## GitHub Pages 測試
- push 後，GitHub Pages 有更新。
- 網頁可以正常載入。
- CSS 樣式正常生效。
- JavaScript 功能正常生效。
- 最新功能可以在 GitHub Pages 網址使用。

## 建議測試流程
1. 清除全部任務。
2. 新增 3 個任務。
3. 勾選 1 個任務完成。
4. 編輯 1 個任務。
5. 確認每個任務的分類正確。
6. 切換分類篩選。
7. 回到完整列表狀態，測試任務上移和下移。
8. 搜尋任務。
9. 切換全部、未完成、已完成篩選。
10. 切換深色模式。
11. 匯出任務資料。
12. 清除全部任務後，再匯入剛才的備份檔。
13. 重新整理頁面。
14. 確認任務、分類、排序、分類篩選、完成狀態、建立日期、主題都仍正確。
15. 測試完成後再 commit 和 push。
